package com.smarttmessenger.app.payments.preferences.viewholder;

import android.view.View;

import androidx.annotation.NonNull;

import com.smarttmessenger.app.payments.preferences.model.InProgress;
import com.smarttmessenger.app.util.adapter.mapping.MappingViewHolder;

public class InProgressViewHolder extends MappingViewHolder<InProgress> {

  public InProgressViewHolder(@NonNull View itemView) {
    super(itemView);
  }

  @Override
  public void bind(@NonNull InProgress model) { }
}
